<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="TabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">@dimen/text_normal</item>
        <item name="android:textColor">@color/colorTextGrey</item>
        <item name="android:textStyle">normal</item>
    </style>

    <style name="confirmDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowTitleStyle">@style/MyTitleTextStyle</item>
        <item name="android:textColorPrimary">#777777</item>
        <item name="android:typeface">monospace</item>
    </style>

    <style name="MyTitleTextStyle">
        <item name="android:textColor">#444444</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Title</item>
        <item name="android:typeface">monospace</item>
    </style>
    <style name="MyDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">

        <item name="android:background">@color/colorSignInBg</item>
        <item name="android:textColor">@color/colorTextGrey</item>
    </style>
</resources>
